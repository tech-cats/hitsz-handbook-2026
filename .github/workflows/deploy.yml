name: Deploy VitePress site to Pages

on:
  push:
    branches: [ci]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Consulta la config real de Pages: con dominio personalizado base_path
      # es "/", sin él es "/<repo>". VitePress exige slash final.
      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v5

      - name: Resolve Pages base path
        id: pages-base
        shell: bash
        run: |
          base="${{ steps.pages.outputs.base_path }}"
          [[ "$base" == */ ]] || base="$base/"
          echo "base=$base" >> "$GITHUB_OUTPUT"

      - name: Build
        run: pnpm docs:build
        env:
          VITEPRESS_BASE: ${{ steps.pages-base.outputs.base }}

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/.vitepress/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
